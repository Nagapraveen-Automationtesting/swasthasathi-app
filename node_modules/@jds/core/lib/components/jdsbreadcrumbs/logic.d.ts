import React, { ReactElement } from 'react';
import { JDSBreadcrumbsProps } from './generated';
import { LinkProps } from '../jdslink/logic';
export interface LinkItem {
    title: string;
    href?: string;
    onClick?: React.MouseEventHandler;
    provider?: JSX.Element;
}
export interface BreadcrumbProps extends Pick<JDSBreadcrumbsProps, 'custom' | 'currentPage'> {
    currentPage: string;
    children?: ReactElement<LinkItem>[];
    links?: LinkProps[];
}
export declare const withLogic: (Component: React.ComponentType<BreadcrumbProps>) => React.FC<BreadcrumbProps>;
