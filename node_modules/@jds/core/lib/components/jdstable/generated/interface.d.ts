import type { JDSTableCellProps } from '../../internal';
export interface JDSTableProps {
    /**  */
    density?: JDSTableCellProps['density'];
    /**  */
    background?: boolean;
    /** Show/hide border between columns. */
    columnDivider?: boolean;
    /**  */
    fullWidth?: boolean;
    /** Pass rows as an array of objects {[HEADER_NAME_1]?: , [HEADER_NAME_2]?:...}, where each row is an object where:
- the properties are equal to header names
- values are displayed in columns
- properties for non-existent header.name are ignored, warning is displayed
- missing header property results in empty column */
    rows?: Array<{
        cells?: Array<any>;
    }>;
    /**  */
    zebra?: boolean;
    /** Optional text at the bottom. */
    footer?: string;
    ariaLabel?: string;
    ariaDescribedby?: string;
    custom?: string;
}
