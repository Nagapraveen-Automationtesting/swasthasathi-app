import { IPROP_MAP } from '../../generators/modifyProps';
import { JDSTooltipProps } from './generated';
declare const legacyTooltipPositions: readonly ["right-start", "right-end", "bottom-start", "bottom-end", "left-start", "left-end", "top-start", "top-end"];
type legacyTooltipPositionsType = (typeof legacyTooltipPositions)[number];
export type TooltipPosition = legacyTooltipPositionsType | JDSTooltipProps['position'];
export interface TooltipProps extends Omit<JDSTooltipProps, 'position' | 'onAppear' | 'offsetX' | 'offsetY' | 'triggerRef' | 'onHover'> {
    position?: TooltipPosition;
    label?: string;
    children?: React.ReactNode;
}
export declare const PROP_MAP: IPROP_MAP<TooltipProps>;
export {};
