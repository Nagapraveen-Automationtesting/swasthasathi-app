import type { JDSFeedbackBlockProps } from '../../internal';
export interface JDSCheckboxProps {
    /** It can be used to programmatically make input active/inactive. */
    checked?: boolean;
    /**  */
    indeterminate?: boolean;
    /** It can be used control the size of Checkbox. */
    size?: 'medium' | 'small';
    /** It can be used to enable or disable Checkbox. */
    disabled?: boolean;
    stateText?: string;
    state?: JDSFeedbackBlockProps['state'];
    /** It displays label of Checkbox. */
    label?: string;
    /** It displays helper text of Checkbox. */
    helperText?: string;
    onClick?: (ev: React.MouseEvent<HTMLButtonElement>) => void;
    ariaLabel?: string;
    ariaDescribedby?: string;
    custom?: string;
}
