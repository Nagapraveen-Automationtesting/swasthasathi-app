import type { JDSAvatarProps, JDSIconProps } from '../../internal';
export interface JDSAccordionProps {
    /**  */
    expandedPanel?: boolean;
    /** It can be used to enable or disable Accordion. */
    disabled?: boolean;
    /**  */
    showDivider?: boolean;
    /** The icon to be displayed. The plus icon changes to a minus icon when the corresponding panel is expanded. */
    iconType?: 'chevron' | 'plus';
    /** This displays the title of Accordion. */
    title?: string;
    /** This displays the subtitle of Accordion. */
    subtitle?: string;
    onClick?: (ev: React.MouseEvent<HTMLButtonElement>) => void;
    children?: React.ReactNode;
    prefix?: React.ReactElement<JDSAvatarProps> | React.ReactElement<JDSIconProps>;
    ariaLabel?: string;
    ariaDescribedby?: string;
    custom?: string;
}
