import type { JDSFeedbackBlockProps } from '../../internal';
export interface JDSToggleProps {
    /** It can be used to programatically make input active/inactive. */
    checked?: boolean;
    /** It can be used to position lable to either left or right of Toggle. */
    labelPosition?: 'left' | 'right';
    /** It can be used control the size of Toggle. */
    size?: 'medium' | 'small';
    /** It can be used to enable or disable Toggle. */
    disabled?: boolean;
    stateText?: string;
    state?: JDSFeedbackBlockProps['state'];
    /** It displays label of Toggle. */
    label?: string;
    /** Take icon input for Toggle. */
    icon?: string | JSX.Element;
    /** It displays helper text of Toggle. */
    helperText?: string;
    onClick?: (ev: React.MouseEvent<HTMLButtonElement>) => void;
    onPrefixClick?: (ev: React.MouseEvent<HTMLButtonElement>) => void;
    ariaLabel?: string;
    ariaDescribedby?: string;
    custom?: string;
}
