import { JDSRatingBarProps } from './generated/interface';
export interface RatingBarProps extends Omit<JDSRatingBarProps, 'onChange' | 'count'> {
    onChange?: (rating: number) => void;
    count?: number;
    ariaLabel?: string;
    ariaDescribedby?: string;
}
export declare const withLogic: (Component: React.ComponentType<RatingBarProps>) => React.FC<RatingBarProps>;
