import { IPROP_MAP } from '../../generators/modifyProps';
import { JDSSidePanelProps } from './generated';
import { ListBlockProps } from '../jdslistblock/logic';
export interface SidePanelProps extends Omit<JDSSidePanelProps, 'header' | 'onClose' | 'platform' | 'items' | 'onContainerClick' | 'onSidePanelSuffixClick'> {
    /**
     * Header to be displayed
     *
     * Passing listblock props are deprecated. Pass a text elment
     */
    header?: JDSSidePanelProps['header'] | ListBlockProps;
    /**
     * Event that gets triggered when `close` button clicked
     */
    onClose?: () => void;
    /**
     * Defines if transition should be disabled, if set to true panel will not slide but appear instantly
     */
    disableTransition?: boolean;
}
export declare const PROP_MAP: IPROP_MAP<SidePanelProps>;
export declare const withLogic: (Component: React.ComponentType<SidePanelProps>) => React.FC<SidePanelProps>;
