import type { JDSTextProps, JDSBadgeProps, JDSTagProps } from '../../internal';
export interface JDSContentBlockProps {
    /** It can be used to control the size of ContentBlock. */
    size?: 'xtra-small' | 'xtra-xtra-small' | 'small' | 'medium' | 'large' | 'xtra-large';
    /** It can be used to either wrap the primary button, secondary button horizontally or vertically. */
    ctaWrap?: 'horizontal' | 'vertical';
    /** It inverts the order of the CTAs if true. */
    invertCTA?: boolean;
    /**  */
    shouldOverrideColor?: boolean;
    /**  */
    singleButton?: boolean;
    /**  */
    isTertiary?: boolean;
    /** It displays the title of ContentBlock. */
    title?: string;
    /** It can be used to add description to the ContentBlock. */
    description?: string;
    /** It displays label of secondary button of Toast. */
    primaryCTA?: string;
    /** It displays label of primary button of Toast. */
    secondaryCTA?: string;
    onPrimaryClick?: (ev: React.MouseEvent<HTMLButtonElement>) => void;
    onSecondaryClick?: (ev: React.MouseEvent<HTMLButtonElement>) => void;
    caption?: React.ReactElement<JDSTextProps> | React.ReactElement<JDSBadgeProps> | React.ReactElement<JDSTagProps>;
    children?: React.ReactNode;
    primaryCtaAriaLabel?: string;
    primaryCtaAriaDescribedby?: string;
    secondaryCtaAriaLabel?: string;
    secondaryCtaAriaDescribedby?: string;
    custom?: string;
}
