import { CarouselSlideProps } from '../jdscarouselslide/logic';
import { JDSCarouselProps } from './generated';
import { ReactElement } from 'react';
export type CarouselProps = {
    /**
     * 	‘normal’ (default) | ‘diminishing’
     */
    dotType?: 'normal' | 'diminishing';
    /**
     * when in tablet/mobile, next slide is partially visible, use this prop to hide it.
     */
    hideNextSlide?: boolean;
    /**
     * The duration the carousel stays at slide before going to next. (only applicable on autoplay) default: 3000ms
     */
    slideDuration?: number;
    /**
     * used to indicate the slide from which carousel starts initially
     */
    initialIndex?: number;
    /**
     * to be used when number of children != total slides (e.g. when lazyloading or server pagination)
     */
    totalSlides?: number;
    /**
     * Method used to go to next slide.
     */
    next?: () => void;
    /**
     * Method used to go to the previous slide.
     */
    prev?: () => void;
    /**
     * Method used to go to a particular slide.
     */
    goToSlide?: (index: number) => void;
    /**
     * 	Method used to access current index of carousel.
     */
    getCurrentIndex?: () => number;
    /**
     * Triggered on the change of slide.
     */
    onChange?: (index: number, action: 'next' | 'prev' | 'manual') => void;
    /**
     * Callback passed by user to generate caption.
     */
    getCaption?: (currentIndex: number, totalSlides: number) => string;
    /**
     * The active index on carousel.
     */
    activeIndex?: number;
    /**
     * Triggered on pause of auto playing slides.
     */
    onPause?: (index: number) => void;
    /**
     * Triggered on resume/play of auto playing slides.
     */
    onPlay?: (index: number) => void;
    /**
     * Triggerred on click of pagination dot
     */
    onPaginationClick?: () => void;
    /**
     * Must be children of type <CarouselSlide/>
     */
    children: ReactElement<CarouselSlideProps> | ReactElement<CarouselSlideProps>[];
} & Omit<JDSCarouselProps, 'onBackBtn' | 'onNextBtn' | 'onDotClick' | 'onClick' | 'onNext' | 'onBack' | 'children'>;
