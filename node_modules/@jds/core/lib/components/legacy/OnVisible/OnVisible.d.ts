import { FC, HTMLAttributes } from 'react';
interface OnVisibleProps extends Omit<HTMLAttributes<HTMLDivElement>, 'onChange'> {
    /**
     * Triggered when an element becomes visible
     */
    onChange?: (visible: boolean) => void;
    /**
     * Percentage of element that should be visible before triggering change.
     * e.g `percent={80}`
     */
    percent?: number;
    /**
     * class name that should be applied when visible
     */
    visibleClassName?: string;
    /**
     * Element of the wrapper that should be used
     * E.g: `span`
     */
    wrappingElement?: string;
}
declare const OnVisible: FC<OnVisibleProps>;
export default OnVisible;
