export interface JDSSkeletonProps {
    /** It is used to determine the shape of Skeleton. */
    shape?: 'circle' | 'heading' | 'paragraph' | 'rectangle' | 'square' | 'custom';
    /** It can be used to add animantionto Skeleton. */
    animated?: boolean;
    /** It can be used to determine width of all shapes od Skeleton. */
    width?: string | number;
    custom?: string;
}
