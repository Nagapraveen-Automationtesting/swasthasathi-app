import { IPROP_MAP } from '../../generators/modifyProps';
import { JDSBottomNavigationProps } from './generated';
import { LinkProps } from '../jdslink/logic';
export type IconLinkType = {
    icon: string | JSX.Element;
} & Omit<LinkProps, 'children'>;
export interface BottomNavProps extends Omit<JDSBottomNavigationProps, 'items' | 'onChange'> {
    /**
     * List of icons and their links. e.g. `IconLink=
     * [{icon:<IcFavorite />}, {icon:<IcAdd />, href: 'news'}]
     * `
     */
    items: Array<IconLinkType & {
        disabled?: boolean;
        label?: string;
    }>;
    /**
     * Current active icon selected. You can overwrite internal active
     * state by passing a new active state.
     */
    activeIndex?: number;
    /**
     * Returns the index of icon when clicked.
     * @param activeIndex
     */
    onChange?: (activeIndex: number) => void;
    /**
     * Shows label below icon if true.
     * Default is false
     */
    showLabel?: boolean;
}
export declare const PROP_MAP: IPROP_MAP<BottomNavProps>;
export declare const withLogic: (Component: React.ComponentType<BottomNavProps>) => React.FC<BottomNavProps>;
