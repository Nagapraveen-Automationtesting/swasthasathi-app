export declare function getStrokeColor(appearance: string, scale: number, surface: string, mode?: string, state?: string, tinted?: boolean): {
    scales: {
        [key: number]: number;
    };
    color?: string;
};
