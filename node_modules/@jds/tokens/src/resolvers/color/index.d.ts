import { getSurfaceScale } from './surface-logic';
import { FlatTokens } from '@jds/tokens-parser';
declare function resolveColorByType(color: string, tokens?: FlatTokens): {
    scales?: {
        [key: number]: number;
    };
    surfaceType?: string;
    color?: string;
};
export { resolveColorByType, getSurfaceScale };
