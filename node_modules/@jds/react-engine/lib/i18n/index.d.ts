/**
 * Retrieves the language from the given locale.
 * @param locale The locale string.
 * @returns The language corresponding to the locale.
 */
export declare function getLanguageFromLocale(locale: string): string;
/**
 * Retrieves an array of month dates based on the specified locale.
 * If no locale is provided, 'en' (English) is used as the default.
 * @param locale The locale to retrieve month dates for.
 * @returns An array of month dates.
 */
export declare function getMonthDates(locale?: string): string[];
/**
 * Retrieves an array of month names based on the specified locale.
 * If no locale is provided, it defaults to 'en'.
 *
 * @param locale The locale for which to retrieve the month names.
 * @returns An array of month names.
 */
export declare function getMonthNames(locale?: string): string[];
/**
 * Retrieves an array of strings for numerals based on the specified locale.
 * If no locale is provided, 'en' is used as the default.
 * @param locale The locale to retrieve numbers for.
 * @returns An array of strings of numerals.
 */
export declare function getNumbers(locale?: string): string[];
/**
 * Retrieves the initials of weekdays based on the specified locale.
 * If no locale is provided, 'en' (English) is used as the default.
 * @param locale The locale for which to retrieve the weekday initials.
 * @returns An array of weekday initials.
 */
export declare function getWeekdayInitials(locale?: string): string[];
/**
 * Retrieves the names of weekdays in the specified locale.
 * If no locale is provided, it defaults to 'en'.
 *
 * @param locale - The locale for which to retrieve the weekday names.
 * @returns An array of weekday names in the specified locale.
 */
export declare function getWeekdayNames(locale?: string): string[];
export declare function getConfirmLabel(locale?: string): string;
export declare function getOf(locale?: string): string;
