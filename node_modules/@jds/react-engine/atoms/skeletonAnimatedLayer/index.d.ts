import { CSSProperties } from 'react';
interface SkeletonAnimatedLayerProps {
    layer: string;
    style: CSSProperties;
    className: string;
}
declare const SkeletonAnimatedLayer: ({ layer, style, className, ...rest }: SkeletonAnimatedLayerProps) => JSX.Element;
export { SkeletonAnimatedLayer };
