import { EventHandler } from 'react';
import { API } from '../lib/interfaces';
export interface SlotProps<P extends Record<string, unknown>> extends React.HTMLAttributes<HTMLDivElement> {
    _childrenProps: {
        api: API['children'];
        props: P;
    };
    layer: string;
    disabled?: boolean;
    events: Record<string, EventHandler<any>>;
    slot_layers: Record<string, unknown>;
}
export declare const Slot: <P extends Record<string, unknown> = Record<string, unknown>>({ _childrenProps, layer, events, className, style, disabled, slot_layers, ...rest }: SlotProps<P>) => JSX.Element | null;
export default Slot;
