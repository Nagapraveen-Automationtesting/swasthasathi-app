interface ArcProps {
    radius: number;
    start: number;
    end: number;
    fill: string;
    thickness: number;
    arcWidth: number;
    className?: string;
    layer?: string;
    animate?: boolean;
    'animation-delay'?: string;
}
declare const Arc: ({ radius, start, end, fill, className, arcWidth, "animation-delay": animationDelay, layer, animate, ...rest }: Partial<ArcProps>) => JSX.Element;
export { Arc };
